GROVEPIEMU BUILD 14/02/19
=========================

Directory contains:
- macOS .app packaged version (grovepiemu.app)
- Single executable file version ('grovepiemu')

The latter can be run on the command line and output / errors monitored
